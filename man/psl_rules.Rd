% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{psl_rules}
\alias{psl_rules}
\title{Rules of the active Public Suffix List}
\usage{
psl_rules(section = c("all", "icann", "private"))
}
\arguments{
\item{section}{Which rule sections to return: \code{"all"} (default), \code{"icann"},
or \code{"private"}.}
}
\value{
A base \link{data.frame} with columns, in order: \code{rule} (original source
rule text), \code{canonical_rule} (the canonicalized rule, including the \verb{*.} or
\code{!} marker), \code{kind} (\code{"normal"}, \code{"wildcard"}, or \code{"exception"}), \code{section}
(\code{"icann"} or \code{"private"}), and \code{labels} (integer rule depth, counting a
wildcard label). Rows are ordered first by section (ICANN before PRIVATE)
and then by source-file order.
}
\description{
Returns the explicit rules of the active list as a base \link{data.frame}, one row
per rule. The implicit default \code{*} rule is not included.
}
\examples{
head(psl_rules("icann"))
nrow(psl_rules("private"))
}
\seealso{
\code{\link[=psl_version]{psl_version()}}, \code{\link[=public_suffix_rule]{public_suffix_rule()}}
}
