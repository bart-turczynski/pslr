% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{psl_outdated}
\alias{psl_outdated}
\title{Is the active Public Suffix List snapshot stale?}
\usage{
psl_outdated(max_age = 180)
}
\arguments{
\item{max_age}{Maximum age, in days, before the active snapshot is considered
outdated. A single positive number; defaults to 180.}
}
\value{
A single logical, with an \code{"age_days"} attribute: \code{TRUE} when the
active list is more than \code{max_age} days old, \code{FALSE} when it is fresher,
and \code{NA} when its date is unknown.
}
\description{
An offline staleness check: compares the \code{list_date} of the list currently
active in this session (see \code{\link[=psl_version]{psl_version()}}) against the current time and
reports whether it is older than \code{max_age} days. The Public Suffix List
changes continually upstream, so a long-lived bundled snapshot drifts from
the live list over time; this is the signal to consider \code{\link[=psl_refresh]{psl_refresh()}}.
}
\details{
The check reads only the already-loaded active metadata: it never touches the
network and never activates a different list. When the active list's
\code{list_date} is unknown (\code{NA}) or cannot be parsed, the result is \code{NA} --
staleness is undetermined rather than assumed either way.

The age of the active snapshot in days is attached to the result as the
\code{"age_days"} attribute (a double, or \code{NA} when the date is unknown), so a
caller that wants the magnitude as well as the verdict need not recompute it.
}
\examples{
# Is the active snapshot older than the default threshold?
psl_outdated()

# The age in days is available without recomputing it:
attr(psl_outdated(), "age_days")

# Use a stricter threshold:
psl_outdated(max_age = 30)
}
\seealso{
\code{\link[=psl_version]{psl_version()}}, \code{\link[=psl_refresh]{psl_refresh()}}
}
