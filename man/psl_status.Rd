% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/status.R
\name{psl_status}
\alias{psl_status}
\alias{format.psl_status}
\alias{print.psl_status}
\title{Offline freshness status of a Public Suffix List snapshot}
\usage{
psl_status(snapshot = "active", ..., now = psl_now())

\method{format}{psl_status}(x, ...)

\method{print}{psl_status}(x, ...)
}
\arguments{
\item{snapshot}{Which snapshot to inspect: \code{"active"} (the list active in
this session), \code{"cache"} (the snapshot \code{psl_use("cache")} would resolve
to), or \code{"bundled"} (the snapshot installed with the package).}

\item{...}{These dots are for future extension and must be empty.}

\item{now}{The instant to evaluate freshness against, as a single POSIXct
time. Defaults to the current time, read once per call. Mainly useful for
testing.}

\item{x}{A \code{psl_status} row, as returned by \code{\link[=psl_status]{psl_status()}}.}
}
\value{
A one-row base \link{data.frame} of class \code{psl_status} with the columns,
in order: \code{state} (character), \code{snapshot} (character, the requested
selector), \code{source_kind} (character: \code{"bundled"}, \code{"cache"}, \code{"path"}, or
\code{"missing"}), \code{request_url} (character), \code{checksum} (character, the
inspected snapshot's identity), \code{source_checksum} (character, the newest
checksum observed for the source), \code{content_date} (POSIXct upstream
provenance date), \code{retrieved_at} (POSIXct), \code{checked_at} (POSIXct),
\code{next_check_at} (POSIXct courtesy boundary), \code{snapshot_age_days}
(double), \code{check_age_days} (double), \code{check_due} (logical), and \code{message}
(character remediation for \code{"missing"} and \code{"unknown"}). Unavailable
values are a typed \code{NA}; ages are \code{NA} when the local clock is behind a
recorded time.

\code{format()} returns a character vector of display lines and \code{print()}
returns \code{x} invisibly.
}
\description{
Reports the strongest freshness claim the locally available evidence
supports about one snapshot: whether it is confirmed current against its
source, whether a check is merely due, whether a newer snapshot has actually
been observed, or whether the local state cannot support any claim at all.
}
\details{
The call is strictly offline and read-only. It makes no request, writes
nothing, and performs no cache migration; unreadable local state is reported
as \code{"unknown"} with remediation rather than raised as an error, so a usable
active list always stays inspectable.

\code{state} is one of, in precedence order:

\describe{
\item{\code{missing}}{The requested cache selection does not exist.}
\item{\code{unknown}}{Required local state is corrupt, ambiguous, or affected
by clock skew; \code{message} carries the remediation.}
\item{\code{untracked}}{The snapshot has no applicable remote source, so no
freshness claim can be made about it.}
\item{\code{update_available}}{A successful earlier check observed a
\emph{different} checksum for the source. This is an observation, never an
inference from age.}
\item{\code{never_checked}}{The source is known but nothing has ever confirmed
the snapshot against it.}
\item{\code{check_due}}{The snapshot was confirmed current, but the retained
reminder interval (7 days by default) has since elapsed. This is offline
advice, not evidence that anything upstream changed.}
\item{\code{confirmed_current}}{The snapshot was confirmed current and the
interval has not elapsed.}
}

Elapsed time alone never produces \code{"update_available"}: an interval that has
passed is \code{"check_due"} and nothing more.
}
\examples{
# The snapshot installed with the package:
psl_status("bundled")

# The list active in this session:
psl_status()

# A cache that was never populated is a status, not an error:
psl_status("cache")$state
}
\seealso{
\code{\link[=psl_refresh]{psl_refresh()}}, \code{\link[=psl_use]{psl_use()}}, \code{\link[=psl_version]{psl_version()}}
}
\keyword{internal}
