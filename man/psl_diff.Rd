% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diff.R
\name{psl_diff}
\alias{psl_diff}
\title{Compare two Public Suffix List snapshots}
\usage{
psl_diff(old, new, ...)
}
\arguments{
\item{old, new}{The two snapshots to compare. Each accepts, independently, one
of: \code{"bundled"} (the snapshot installed with the package), \code{"cache"} (the
snapshot \code{\link[=psl_use]{psl_use()}} would resolve, i.e. the current cache selection), a
path to a local PSL source file, a \code{psl_engine} from \code{\link[=psl_engine]{psl_engine()}}, or a
rule table with the \code{\link[=psl_rules]{psl_rules()}} schema. \code{"bundled"} and \code{"cache"} are
reserved names, so a file with either of those names must be given as a
path carrying a directory component.}

\item{...}{These dots are for future extension and must be empty.}
}
\value{
A base \link{data.frame} with one row per changed logical identity,
ordered by that identity, and columns, in order: \code{change} (\code{"added"},
\code{"removed"}, or \code{"changed"}), \code{rule} (the logical identity), \code{old_rule} and
\code{new_rule} (canonical rule text including any \verb{*.} or \code{!} marker),
\code{old_kind} and \code{new_kind} (\code{"normal"}, \code{"wildcard"}, or \code{"exception"}), and
\code{old_section} and \code{new_section} (\code{"icann"} or \code{"private"}). Every field
belonging to a side the identity is absent from is \code{NA}. Two snapshots that
agree return the same columns and types with zero rows.

Provenance is attached as the attributes \code{old_version} and \code{new_version},
each a one-row \code{\link[=psl_version]{psl_version()}} frame describing that side's snapshot, or
\code{NULL} when the input was a rule table and therefore carries no provenance.
}
\description{
Reports which rules were added, removed, or changed between two Public Suffix
List snapshots that are already available locally. The comparison is
canonical rather than textual: both sides are parsed and canonicalized first,
so comments, blank lines, whitespace, letter case, source ordering, and raw
Unicode spelling are never reported as changes.
}
\details{
The call is strictly offline. No input form triggers a request, and neither
side changes the session-global list that \code{\link[=psl_use]{psl_use()}} controls, so diffing
the cache leaves the next query answering from exactly the list it did
before.
}
\section{Which snapshots are available}{

The upstream endpoint publishes only the current list, and the list itself
asks callers not to depend on version-control URLs, so \code{psl_diff()} resolves
no dates and downloads no history. Installing the package performs no
request and provides only the bundled snapshot; local collection begins at
the first explicit \code{\link[=psl_refresh]{psl_refresh()}}, after which each distinct validated
download is retained as an immutable file until \code{\link[=psl_cache_prune]{psl_cache_prune()}} collects
it. \code{\link[=psl_snapshots]{psl_snapshots()}} lists what is resolvable, with a \code{path} for each set of
stored bytes; any of those paths, or any historical revision the caller
materializes as a file by other means, can be passed here directly.
}

\section{Change semantics}{

A rule's \emph{logical identity} is its canonical labels with the leading \verb{*.} or
\code{!} marker removed, so \verb{*.example.com} and \code{example.com} are the same
identity in two different states. \code{change} is:

\describe{
\item{\code{added}}{The identity exists only in \code{new}.}
\item{\code{removed}}{The identity exists only in \code{old}.}
\item{\code{changed}}{The identity exists in both, but its canonical rule kind
or its ICANN/PRIVATE section differs.}
}

Identities that exist in both snapshots in the same state are not reported.
Section membership is a compared attribute rather than part of the identity,
so a rule moving between the ICANN and PRIVATE sections is one \code{changed} row
rather than an unrelated \code{removed}/\code{added} pair. A list may legally carry the
same labels once in each section; such rules collapse into the single row for
their identity, with the values of that side joined in ICANN-then-PRIVATE
order.
}

\examples{
# Two small lists on disk, written offline.
write_list <- function(private) {
  path <- tempfile(fileext = ".dat")
  writeLines(
    c(
      "// ===BEGIN ICANN DOMAINS===",
      "com",
      "// ===END ICANN DOMAINS===",
      "// ===BEGIN PRIVATE DOMAINS===",
      private,
      "// ===END PRIVATE DOMAINS==="
    ),
    path
  )
  path
}
old <- write_list(c("a.example.com", "*.b.example.com"))
new <- write_list(c("b.example.com", "c.example.com"))

# One row each: a.example.com removed, b.example.com changed kind,
# c.example.com added.
psl_diff(old, new)

# Agreeing snapshots return the same columns with zero rows.
psl_diff("bundled", "bundled")

# Provenance travels with the result when the input form carries it.
attr(psl_diff("bundled", old), "old_version")$checksum
}
\seealso{
\code{\link[=psl_snapshots]{psl_snapshots()}}, \code{\link[=psl_refresh]{psl_refresh()}}, \code{\link[=psl_rules]{psl_rules()}}, \code{\link[=psl_version]{psl_version()}}
}
