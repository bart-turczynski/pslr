% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reminder.R
\name{psl_reminder}
\alias{psl_reminder}
\alias{format.psl_reminder}
\alias{print.psl_reminder}
\title{Persistent opt-in freshness reminder}
\usage{
psl_reminder(enable = NULL, every = NULL)

\method{format}{psl_reminder}(x, ...)

\method{print}{psl_reminder}(x, ...)
}
\arguments{
\item{enable}{Whether attach reminders are on: \code{TRUE} to enable, \code{FALSE} to
disable, or \code{NULL} (the default) to report the current preference without
writing anything.}

\item{every}{The reminder interval in whole days, at least one. \code{NULL} (the
default) reuses the stored interval, or 7 days when no preference has been
stored yet. Only meaningful together with \code{enable}.}

\item{x}{A \code{psl_reminder} preference, as returned by \code{\link[=psl_reminder]{psl_reminder()}}.}

\item{...}{Passed on to the data frame methods when \code{x} no longer carries
the full preference contract.}
}
\value{
A one-row base \link{data.frame} of class \code{psl_reminder} with the columns
\code{enabled} (logical), \code{interval} (integer days), and \code{stored} (logical,
whether the value came from a stored preference rather than the defaults).
Returned visibly when querying and invisibly when writing.

\code{format()} returns a character vector of display lines and \code{print()}
returns \code{x} invisibly.
}
\description{
Queries or sets whether attaching pslr with \code{library(pslr)} may print one
offline freshness reminder per R session, and how many days of unconfirmed
freshness it takes before that reminder applies.
}
\details{
Reminders are off until you turn them on. The preference is configuration
rather than cache: it is stored under \code{\link[tools:R_user_dir]{tools::R_user_dir()}}\verb{("pslr", "config")}, so refreshing, pruning, or deleting the snapshot cache never
changes it. Disabling retains the interval, so a later
\code{psl_reminder(enable = TRUE)} restores the schedule you chose.

Querying, enabling, and disabling are all strictly offline; no part of the
reminder path makes a network request.

When reminders are enabled, a \emph{direct} \code{library(pslr)} attachment evaluates
\code{\link[=psl_status]{psl_status()}} offline and may emit a single \code{\link[=packageStartupMessage]{packageStartupMessage()}}. A
package that merely imports the namespace, as in \code{pslr::public_suffix()},
never triggers it, \code{suppressPackageStartupMessages()} suppresses it as
usual, and it is emitted at most once per session -- detaching and
reattaching does not repeat it.

The message appears only for the three states where local evidence supports
advice: \code{"never_checked"}, \code{"check_due"}, and \code{"update_available"}. For
\code{"update_available"} the newer snapshot is already stored locally, so the
message suggests activating it with \code{psl_use("cache")} rather than fetching
anything again. Any failure while evaluating the reminder -- damaged cache
state included -- degrades to silence, so attaching the package cannot break.
}
\examples{
# Query the current preference; this writes nothing.
psl_reminder()

# Enabling writes to your config directory, so this example points that
# seam at a temporary directory instead.
old <- options(pslr.config_dir = file.path(tempdir(), "pslr-reminder"))

psl_reminder(enable = TRUE, every = 14)
psl_reminder()

# Disabling retains the interval for a later re-enable.
psl_reminder(enable = FALSE)$interval

options(old)
}
\seealso{
\code{\link[=psl_status]{psl_status()}}, \code{\link[=psl_refresh]{psl_refresh()}}, \code{\link[=psl_use]{psl_use()}}
}
\keyword{internal}
