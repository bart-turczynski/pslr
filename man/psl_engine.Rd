% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matcher.R
\name{psl_engine}
\alias{psl_engine}
\alias{print.psl_snapshot}
\alias{print.psl_engine}
\title{Construct a self-contained PSL engine}
\usage{
psl_engine(source = "bundled", path = NULL)

\method{print}{psl_snapshot}(x, ...)

\method{print}{psl_engine}(x, ...)
}
\arguments{
\item{source}{Where to load the list from: \code{"bundled"} (the pinned package
snapshot) or \code{"path"} (a custom file).}

\item{path}{For \code{source = "path"}, a single readable PSL-format UTF-8 file
containing one complete ICANN section and one complete PRIVATE section,
using official markers. Must be \code{NULL} for any other source.}
}
\value{
An engine object that the query functions can be pointed at (via
their \verb{engine=} argument) to resolve hosts against the chosen snapshot in
isolation from the session-global list.
}
\description{
Builds a process-local Public Suffix List engine bound to a specific
snapshot, without switching the session-global active list that \code{\link[=psl_use]{psl_use()}}
controls.
}
\details{
The engine is \strong{process-local}: its compiled matcher is a C++ external
pointer that does not serialize across R sessions or parallel workers.
Saving and reloading an engine, or sending one to a worker, does not carry
the matcher. To persist an engine, serialize its snapshot descriptor and
rebuild the engine from it in the target process.
}
\examples{
engine <- psl_engine("bundled")
engine

# A custom list from a file, entirely offline.
dat <- tempfile(fileext = ".dat")
writeLines(
  c(
    "// ===BEGIN ICANN DOMAINS===",
    "com",
    "// ===END ICANN DOMAINS===",
    "// ===BEGIN PRIVATE DOMAINS===",
    "example.com",
    "// ===END PRIVATE DOMAINS==="
  ),
  dat
)
psl_engine("path", path = dat)
}
\seealso{
\code{\link[=psl_use]{psl_use()}}, \code{\link[=psl_version]{psl_version()}}
}
\keyword{internal}
