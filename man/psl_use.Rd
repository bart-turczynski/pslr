% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refresh.R
\name{psl_use}
\alias{psl_use}
\title{Choose the active Public Suffix List for this session}
\usage{
psl_use(source = c("bundled", "cache", "path"), path = NULL)
}
\arguments{
\item{source}{Where to load the list from: \code{"bundled"} (the pinned package
snapshot), \code{"cache"} (the latest successfully validated snapshot from
\code{\link[=psl_refresh]{psl_refresh()}}), or \code{"path"} (a custom file).}

\item{path}{For \code{source = "path"}, a single readable PSL-format UTF-8 file
containing one complete ICANN section and one complete PRIVATE section,
using official markers. Must be \code{NULL} for any other source.}
}
\value{
Invisibly, the \code{\link[=psl_version]{psl_version()}} row for the newly active list.
}
\description{
Switches the list backing every query in the current R session. The change is
session-only and is validated before any session state changes; a failure
leaves the previously active list usable. A successful switch invalidates the
match-result cache.
}
\details{
A custom path is held to the same runtime duplicate policy as
\code{\link[=psl_refresh]{psl_refresh()}}: exact same-section duplicates warn once and are
deduplicated, while conflicting rule kinds for the same labels are fatal.
Cache and custom-path sources are read in source form and indexed under the
runtime normalizer; they never reuse the bundled generated index.
}
\examples{
psl_use("bundled")
if (interactive()) {
  psl_use("cache")
  psl_use("path", path = "my_list.dat")
}
}
\seealso{
\code{\link[=psl_refresh]{psl_refresh()}}, \code{\link[=psl_version]{psl_version()}}, \code{\link[=psl_rules]{psl_rules()}}
}
