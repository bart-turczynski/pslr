% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshots.R
\name{psl_snapshots}
\alias{psl_snapshots}
\alias{format.psl_snapshots}
\alias{print.psl_snapshots}
\title{Inventory of the locally available Public Suffix List snapshots}
\usage{
psl_snapshots(..., verify = FALSE)

\method{format}{psl_snapshots}(x, ...)

\method{print}{psl_snapshots}(x, ...)
}
\arguments{
\item{...}{These dots are for future extension and must be empty.}

\item{verify}{Whether to rehash every snapshot's bytes to detect corruption
that preserves the byte count. \code{FALSE} (the default) classifies rows from
metadata alone; \code{TRUE} reads every stored snapshot in full.}

\item{x}{A \code{psl_snapshots} inventory, as returned by \code{\link[=psl_snapshots]{psl_snapshots()}}.}
}
\value{
A base \link{data.frame} of class \code{psl_snapshots}, one row per distinct
checksum, ordered by \code{checksum}, with the columns, in order: \code{checksum}
(character, the \code{"sha256:<hex>"} identity), \code{path} (character, the
preferred storage location, \code{NA} when the bytes are not stored), \code{size}
(integer bytes), \code{content_date} (POSIXct upstream provenance date),
\code{first_retrieved_at} (POSIXct, when these bytes were first received),
\code{origin_url} (character immutable origin recorded at publication),
\code{normalization_profile} (character), \code{bundled} (logical, installed with the
package), \code{selected_cache} (logical, the snapshot \code{psl_use("cache")} would
resolve to), \code{active} (logical, the snapshot active in this session),
\code{current_for_any_source} (logical, named by at least one source's current
record), \code{source_count} (integer number of sources associated with these
bytes), and \code{integrity} (character, see Details). Unavailable values are a
typed \code{NA}. The bundled snapshot is always inventoried, so the result is
never empty on a working installation.

\code{format()} returns a character vector of display lines and \code{print()}
returns \code{x} invisibly.
}
\description{
Lists every set of Public Suffix List bytes this installation can resolve:
the snapshot installed with the package plus every snapshot published into
the user cache. There is exactly one row per distinct SHA-256 checksum, so
bytes stored in more than one place collapse into a single row with one
preferred path.
}
\details{
The call is strictly offline and read-only. It makes no request, writes
nothing, repairs nothing, and performs no cache migration; a snapshot whose
local storage is damaged is reported through \code{integrity} rather than raised
as an error.

\code{integrity} is one of:

\describe{
\item{\code{ok}}{Bytes and a readable descriptor are both present and agree.}
\item{\code{missing}}{The bytes or the descriptor are not stored, which is also
what a reference to an already pruned snapshot reports.}
\item{\code{checksum_mismatch}}{The stored bytes cannot be the bytes the
checksum names.}
\item{\code{unknown_schema}}{The descriptor is unreadable, or was written by a
release whose schema this one does not support.}
}

Descriptors are checked structurally; no list is reparsed, so an ordinary
call is cheap enough to make casually. By default \code{checksum_mismatch} is
derived from metadata alone -- a stored file whose size differs from the size
its descriptor recorded cannot hold the bytes the checksum names. That misses
corruption that preserves the byte count; \code{verify = TRUE} rehashes every
snapshot's bytes and catches it, at the cost of reading every stored file in
full.

Request URLs of custom sources may be private, so association with sources
is reported only as the count \code{source_count}. The inventory never returns a
source URL; \code{origin_url} is immutable provenance recorded in the snapshot's
own descriptor.
}
\examples{
# Every snapshot this installation can resolve, without any network access:
snapshots <- psl_snapshots()
snapshots[c("checksum", "bundled", "size", "integrity")]

# The snapshot installed with the package is always inventoried:
snapshots[snapshots$bundled, ]$integrity

# Rehash every stored snapshot instead of trusting recorded sizes:
psl_snapshots(verify = TRUE)$integrity
}
\seealso{
\code{\link[=psl_status]{psl_status()}}, \code{\link[=psl_refresh]{psl_refresh()}}, \code{\link[=psl_diff]{psl_diff()}},
\code{\link[=psl_cache_prune]{psl_cache_prune()}}
}
\keyword{internal}
